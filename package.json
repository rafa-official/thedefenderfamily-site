{
  "name": "the-defender-family",
  "version": "1.0.0",
  "description": "The Defender Family — Real stories from the road",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
